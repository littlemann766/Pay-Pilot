name: Apply Pay-Pilot Update

on:
  push:
    paths:
      - 'Pay-Pilot_update.zip'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pay-pilot-apply-update-main
  cancel-in-progress: false

jobs:
  apply-update:
    runs-on: ubuntu-latest

    steps:
      - name: Check out latest main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Apply uploaded Pay-Pilot package
        shell: bash
        run: |
          set -euo pipefail

          if [ ! -f Pay-Pilot_update.zip ]; then
            echo "No Pay-Pilot_update.zip found."
            exit 0
          fi

          cp Pay-Pilot_update.zip /tmp/Pay-Pilot_update.zip

          git fetch origin main
          git reset --hard origin/main
          cp /tmp/Pay-Pilot_update.zip Pay-Pilot_update.zip

          rm -rf .pay_pilot_update_tmp
          mkdir .pay_pilot_update_tmp
          unzip -o Pay-Pilot_update.zip -d .pay_pilot_update_tmp

          DELETE_LIST=".pay_pilot_update_tmp/.mybills-delete-list.txt"
          if [ -f "$DELETE_LIST" ]; then
            while IFS= read -r OLD_PATH || [ -n "$OLD_PATH" ]; do
              case "$OLD_PATH" in
                ''|'#'*) continue ;;
              esac
              if [[ "$OLD_PATH" = /* || "$OLD_PATH" = *".."* ]]; then
                echo "::error::Unsafe cleanup path: $OLD_PATH"
                exit 1
              fi
              rm -rf -- "$OLD_PATH"
            done < "$DELETE_LIST"
            rm -f "$DELETE_LIST"
          fi

          cp -a .pay_pilot_update_tmp/. .
          rm -rf .pay_pilot_update_tmp
          rm -f Pay-Pilot_update.zip

      - name: Verify update before commit
        shell: bash
        run: |
          set -euo pipefail

          test -f VERSION.txt
          test -f app/build.gradle
          test -f app/src/main/assets/index.html
          test -f app/src/main/assets/mybills.css

          EXPECTED_VERSION="$(awk '/^Version:/{print $2}' VERSION.txt)"
          test -n "$EXPECTED_VERSION"

          grep -F "versionName '$EXPECTED_VERSION'" app/build.gradle
          grep -F "$EXPECTED_VERSION" app/src/main/assets/index.html >/dev/null

          echo "Applied Pay-Pilot $EXPECTED_VERSION preservation update"

      - name: Commit and push applied update
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add -A

          if ! git diff --cached --quiet; then
            VERSION="$(awk '/^Version:/{print $2}' VERSION.txt)"
            git commit -m "Apply Pay-Pilot ${VERSION} update"
            git pull --rebase origin main
            git push origin HEAD:main
          else
            echo "No repository changes to commit."
          fi
